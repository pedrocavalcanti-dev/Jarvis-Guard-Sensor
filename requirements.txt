# Jarvis Guard Sensor — dependências
# Instalar com: pip install -r requirements.txt

# HTTP requests para enviar eventos ao Jarvis Guard
requests>=2.28.0

# Terminal colorido (menu TUI)
colorama>=0.4.6